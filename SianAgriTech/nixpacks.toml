echo '[phases.setup]
cmds = ["apt-get update && apt-get install -y libatomic1"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo "Build complete""]

[start]
cmd = "npm start"

[build.args]
NODE_VERSION = "18.0.0"' > nixpacks.toml